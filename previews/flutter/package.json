{
  "name": "@claude-canvas/preview-flutter",
  "version": "0.1.0",
  "private": true,
  "description": "Flutter Web preview for ClaudeCanvas",
  "type": "module",
  "scripts": {
    "build:flutter": "flutter build web --release --web-renderer html",
    "dev": "serve build/web -l 4202 --cors",
    "build": "npm run build:flutter",
    "preview": "serve build/web -l 4202 --cors"
  },
  "devDependencies": {
    "serve": "^14.2.0"
  }
}
